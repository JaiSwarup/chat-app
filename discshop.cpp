#include<bits/stdc++.h> 
using namespace std; 
#define MOD 1e9 + 7 
#define forn(a, c) for (int(a) = 0; (a) < (c); (a)++) 
#define forsn(a, b, c) for (int(a) = (b); (a) <= (c); (a)++) 
#define forrn(a, b) for (int(a) = (b); (a) >= 0; (a)--) 
#define forrs(a, b, c) for (int(a) = (b); (a) >= (c); (a)--) 
#define INF 1000000000000000003 
typedef long long int ll; 
typedef vector<int> vi; 
typedef vector<ll> vl; 
typedef vector<vector<int>> vivi; 
typedef pair<int, int> pi; 
typedef pair<ll, ll> pll; 
#define fi first 
#define se second 
#define pb push_back 
#define pob pop_back 
#define MP make_pair 
#define all(c) c.begin(), c.end() 
#define sz(x) (int)x.size() 
#define ln "\n" 
#define debug(x) cout << #x << " is " << x << ln; 
#define debugarr(a, n) for (int i = 0; i < n; i++) cout << a[i] << " "; cout << ln; 
#define readarr(a, n) for (int i = 0; i < n; i++) cin >> a[i]; 
#define fast ios::sync_with_stdio(0); cin.tie(0); cout.tie(0)

void solve() { 
    int n;
    cin >> n;

    
} 
int main() 
{ 
    fast; 
    int T = 1; 
    cin >> T; 
    while (T--) { 
        solve();
    } 
    return 0; 
} 